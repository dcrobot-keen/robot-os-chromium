{
  "name": "ros-chromium-web",
  "version": "0.0.1",
  "private": true,
  "description": "Browser-side stack: hardware bridge, device abstraction, nodes, comms, HMI. See research.md and plan.md.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "test": "node scripts/roboteq-smoke.mjs && node scripts/signaling-smoke.mjs && node scripts/planner-wasm-smoke.mjs"
  }
}
