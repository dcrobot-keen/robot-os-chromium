{
  "name": "@ros-chromium/rtc",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "WebRTC data channel + signaling client for remote sessions (plan.md Phase 5). The operator side (RtcTransport) implements the same HardwareTransport shape as WebSocketTransport, so the dashboard drives a remote robot with the same code it uses locally. Not on the critical path for local teleop.",
  "main": "src/index.js"
}
