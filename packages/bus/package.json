{
  "name": "@ros-chromium/bus",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "LocalBus (topic-namespaced pub/sub over BroadcastChannel) and HardwareBridgeClient + hardware-bridge-worker.js: the SharedWorker that owns the one real hardware connection across every tab (plan.md Phase 4).",
  "main": "src/index.js"
}
