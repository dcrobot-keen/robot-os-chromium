{
  "name": "@ros-chromium/device-abstraction",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Manifest loader plus createDriveDevice — a WoT-style drive action whose wire command vocabulary is read from the manifest (drive.commands/channels/scale), so a new differential base on the same wire protocol needs no code change. See ../../manifests/former.manifest.json and ../../../former-motor-protocol.md.",
  "main": "src/index.js"
}
